
#### Env
az keyvault secret set --vault-name $keyvaultname --name "AIService--Key" --value "" --description "OpenAI Key for AI Service"
az keyvault secret set --vault-name $keyvaultname --name "AppInsightsWorkSpaceID" --value "tbd" --description "Application Insights Workspace ID"
az keyvault secret set --vault-name $keyvaultname --name "Aspose--PrivateKey" --value "" --description "Private Key for Aspose API"
az keyvault secret set --vault-name $keyvaultname --name "Aspose--PublicKey" --value "" --description "Public Key for Aspose API"
az keyvault secret set --vault-name $keyvaultname --name "Authentication--AzureAd--ClientSecret" --value "" --expires --description "API App Registration Client Secret for Authentication"
az keyvault secret set --vault-name $keyvaultname --name "Azure--SignalR--ConnectionString" --value "" --description "SignalR Connection String for Azure SignalR Service"
az keyvault secret set --vault-name $keyvaultname --name "AzureSpeech--Key" --value "" --description "Azure Speech Service Key"
az keyvault secret set --vault-name $keyvaultname --name "ChatStore--Cosmos--ConnectionString" --value ""
az keyvault secret set --vault-name $keyvaultname --name "ChatStore--Cosmos--ConnectionString--PromoteTarget" --value ""
az keyvault secret set --vault-name $keyvaultname --name "ChatStore--Cosmos--ConnectionString--PublishTarget" --value ""
az keyvault secret set --vault-name $keyvaultname --name "EmailService--Key" --value "tbd" --description "SendGrid Key for Email Service"
az keyvault secret set --vault-name $keyvaultname --name "EYQ--OCP--ClientId" --value "" --description "EYQ API Client ID for x-client-id - Request to EYQ"
az keyvault secret set --vault-name $keyvaultname --name "EYQ--OCP--Key" --value "" --description "EYQ API Key for x-client-secret - Request to EYQ"
az keyvault secret set --vault-name $keyvaultname --name "GtpIt--CopilotAuth--Secret" --value "" --description "GtpIt Sidecar App Reg Secret"
az keyvault secret set --vault-name $keyvaultname --name "GtpIt--DocumentIntegration--DownloadPath" --value "" --description "GtpIt SideCar Document Integration Download Path"
az keyvault secret set --vault-name $keyvaultname --name "GtpIt--DocumentIntegration--UploadPath" --value "" --description "GtpIt SideCar Document Integration Upload Path"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--ServiceAuthorization--AccessKey1" --value "" --description "Kernel API GUID Option 1"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--ServiceAuthorization--AccessKey2" --value "" --description "Kernel API GUID Option 2"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--AzureAIDocIntel--APIKey" --value "" --description "Azure AI Document Intelligence API Key"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--AzureBlobs--ConnectionString" --value "" --description "Azure Blob Storage (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--AzureOpenAIEmbedding--APIKey" --value "" --description "OpenAI East US 2 Key"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--AzureOpenAIText--APIKey" --value "" --description "OpenAI East US 2 Key"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--AzureQueue--ConnectionString" --value "" --description "Azure Queue Storage (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemory--Services--Postgres--ConnectionString" --value "" --description "ADO.NET PostgreSQL Connection String"
az keyvault secret set --vault-name $keyvaultname --name "KernelMemoryService--Key" --value "" --description "Kernel API GUID Option 1"
az keyvault secret set --vault-name $keyvaultname --name "MemoryStore--Postgres--ConnectionString" --value "" --description "ADO.NET PostgreSQL Connection String"
az keyvault secret set --vault-name $keyvaultname --name "MemoryStore--Postgres--ConnectionString--PromoteTarget" --value "" --description "ADO.NET PostgreSQL Connection String"
az keyvault secret set --vault-name $keyvaultname --name "MemoryStore--Postgres--ConnectionString--PublishTarget" --value "" --description "ADO.NET PostgreSQL Connection String"
az keyvault secret set --vault-name $keyvaultname --name "OcrSupport--AzureFormRecognizer--Key" --value "" --description "Azure AI Document Intelligence API Key"
az keyvault secret set --vault-name $keyvaultname --name "OcrSupport--DocumentContentExtractorFormRecognizer--Key" --value "" --description "Azure AI Document Intelligence API Key"
az keyvault secret set --vault-name $keyvaultname --name "Plugins--AzureAd--ClientSecret" --value "" --expires --description "Plugins App Registration Client Secret for Plugins"
az keyvault secret set --vault-name $keyvaultname --name "RedisCache--ConnectionString" --value ""
az keyvault secret set --vault-name $keyvaultname --name "ServiceBus--ConnectionString" --value "tbd" --description "General Managed Keys Connection String"
az keyvault secret set --vault-name $keyvaultname --name "SharePointClient--ClientSecret" --value "" --description "Sharepoint App Registration Client Secret - Need to discuss with Neel"
az keyvault secret set --vault-name $keyvaultname --name "StorageAccount--ConnectionString" --value "" --description "Lab Azure Blob Storage (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "StorageAccount--ConnectionString--PromoteTarget" --value "" --description "Pilot Azure Blob Storage (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "StorageAccount--ConnectionString--PublishTarget" --value "" --description "Prod Blob Storage (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "Logging--ApplicationInsights--ConnectionString" --value "" --description "ApplicationInsights (App) Connection String"
az keyvault secret set --vault-name $keyvaultname --name "FuncAppStorageConnectionString" --value "" --description "App Storage Account Connection String without Suffix"

